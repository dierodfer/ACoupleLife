; Proyecto personal: registro público de npm.
;
; El ~/.npmrc de esta máquina apunta al Artifactory corporativo, que devuelve
; 403 en paquetes como zustand. Este .npmrc de proyecto lo sobrescribe sin
; tocar la configuración global, así que los repos de trabajo siguen igual.
registry=https://registry.npmjs.org/
